# TPCA Requirements
# Phase 1 + Phase 2 + Phase 3
#
# Install all:
#   pip install -r requirements.txt
#
# Minimal install (Python only, no LLM synthesis):
#   pip install tree-sitter-python networkx

# ── Pass 1 — AST Indexing ────────────────────────────────────────────────────
tree-sitter>=0.25.0
tree-sitter-python>=0.25.0

# ── NEW (Phase 3) — Multi-Language Parsing ───────────────────────────────────
# Install to enable JavaScript / TypeScript / TSX indexing.
# Python-only setups can omit these; the indexer degrades gracefully.
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0

# ── Graph construction and ranking ───────────────────────────────────────────
networkx>=3.0
numpy==2.4.3
scipy==1.17.1

# ── Pass 2 — LLM Client ──────────────────────────────────────────────────────
anthropic>=0.25.0
tiktoken>=0.6.0

# ── Pass 2 — Ollama provider (optional) ──────────────────────────────────────
# Used when config.provider == 'ollama'
openai>=1.0.0

# ── CLI ──────────────────────────────────────────────────────────────────────
click>=8.0.0
prompt_toolkit>=3.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0
